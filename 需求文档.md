# Paperfect 需求文档

## 项目背景
看学术论文时，最痛苦的不是英文，而是阅读节奏割裂。比如看 PDF 原文，经常想对照看对应的 PPT 汇报页面；或者看 PPT 讲义时，找不到 PDF 里具体的出处。另外，论文中的公式、图表非常多，普通的翻译工具直接翻译会导致排版完全崩塌。

为了实现“非常完美 (Paperfect)”的阅读体验，我们需要做一个智能论文阅读与管理系统。

## 核心需求
1. **双向幻灯片-文献同步**：
   - PPT 视窗切换幻灯片时，PDF 视窗自动滚动到对应的页码。
   - PDF 视窗滚动阅读时，PPT 视窗自动翻页跟读。
2. **学术阅读四色编码标注**：
   - 自动在 PDF 上打上四色高亮或下划线：
     - 红色 (波浪线)：前人局限与挑战。
     - 黄色 (高亮)：核心创新与贡献。
     - 蓝色 (下划线)：特定网络架构与指标。
     - 绿色 (便签)：该页大意总结。
3. **AI 自动命名与论文管理**（合并自 papermanager 需求）：
   - 自动识别导入的 PDF 标题并规范命名。
   - 根据期刊分区打标签（南大核心、北大核心、SCI、CCF分区等）。
   - 自动查询 IF 影响因子。
   - 根据关键词 keywords 在平面上生成类似 Obsidian 的小球关系网图谱。
4. **体验优化需求**：
   - 黑暗模式支持：只取反文本背景，不能影响插图。
   - 更好的 PDF 编辑器，支持划词翻译。

---

## 附：原始复盘记录 (Obsidian 导入)

# 当前问题v1.1
目前该项目已经能够正常运行，但是有一些问题需要改进
## 1.耦合度问题
首先是代码的耦合度太高
所有功能，不管多小都拆开
a/route.ts
b/route.ts
可能还需要引入路由树
## 2.废弃代码问题
像是E:\workspace\ddl\standalone_pdf2ppt\llm_client.py
其中用于剧本生成的代码和本项目毫无关系，需要删除
## # 项目代码重构需求
## 服务端（前后端），数据端分离
整个文件应该是
```python
ddl/
├── frontend/
├── backend/
├── data/
├── .gitignore
├── README.md
├── API.md
├── .env
├── .env.example
```
## 数据结构重构
对于论文及其处理数据E:\workspace\ddl\papers
目前能明显看出数据结构混乱，应该做到
```python
├── data/
│   ├── papers/             # 用于储存论文处理的数据
│   │   ├── raw/            # 原始论文PDF
│   │   ├── parsed/         # 处理后的论文Markdown（输出结果，要注意目前输出结果和knowledgebase内容一致，只是用途不一样，所以需要更改调用路径然后只保留一个文件夹）
│   │   ├── translated/     # 翻译后的论文PDF
│   │   │   └── cache/          # 缓存文件
│   │   ├── marked/         # 标注PDF
│   │   ├── images              # 要注意目前的images和figures内容一致，只是用途不一样，所以需要更改调用路径然后只保留一个文件夹
│   │   └── pptx                # 保存输出的pptx文件
│   └── textbooks/          #用于储存教材处理的数据
│       ├── raw/            # 原始教材PDF
│       ├── parsed/         # 处理后的教材Markdown
│       └── cache/          # 缓存文件
```
## 项目服务
# 论文梳理流程优化
目前的处理流程都还处于线性流程，这显然效率低下
## 目前大致流程为
1.翻译
2.解析
3.制作PPT
4.论文标注
## 我建议更改为
1.翻译+解析（解析和翻译没有依赖关系，可以同时进行）
2.制作PPT+论文标注（制作PPT和论文标注都只是依赖解析文档，可以同时进行）
# 服务端梳理（用于管理端口，可能有遗漏）
## 论文解析
## 论文翻译
## 论文标注
## ppt生成
## 知识库问答

# 项目新需求
首页目前都是论文库的排列，显然太乱，因此我们需要做论文库的管理界面
以下是我们目标初步实现的功能
## 1.ai自动识别论文标题并命名
用户导入的pdf未必是论文名称
## 2.按照期刊对应自动分区（多个标准打标签）
### 1.普刊
南大核心
北大核心
中文核心
ei会议
.....
### 2.论文用途
本科论文/研究生论文/博士论文/项目论文
### 3.SCI/SSCI分区
中科院分区
JCR分区
### CCF分区
## 3.按照论文研究领域分类
## 4.按照文章keywords进行分类
## 5.自动查询论文IF（影响因子）
## 6.AI辅助进行论文检索
可以通过包括但不限于
论文研究内容，论文研究领域，论文大致研究思路/研究思路类似等信息借助AI进行检索
## 7.根据keywords对论文进行可视化的关系图谱梳理（类似参考obsidian）
平面上每篇论文都是一个个小球，小球上插着代表不同keywords的签子，相同的keywords会连接，有一定数量的同一/相关领域论文之后会形成一个基于keywords的网，方便我们快速了解我们可能会涉及的领域
## PS：要想实现这样非常好的论文管理，可能需要采用数据库，因此需要比较好（结构清晰）的数据库设计
# 其他的一些难以归类需求(更改的第一版先不着急做)
## 1.黑暗模式的PDF页面处理
对于文本PDF，整体界面除图片以外的背景颜色取反（e.g.白纸黑字→黑纸白字），但是图片不可以取反
## 2.界面主题管理
我很希望程序的界面颜色搭配设计可以参考IDE
因此需要像IDE一样，给不同形式的信息编写颜色规范
## 3.更好的PDF编辑器
目前我们还不支持划词翻译，后续考虑通过自主编写/魔改已有的pdf编辑工具来实现这个功能




# 开源内容
## 处理论文提示词


---

## ️ 交互脑图构思 (Obsidian 导入)

1.使用论文呢图片做ppt
2.没有针对学术论文（doi之类的）
3.需求：有时候想把论文做分类，有的单分类有的多分类
kimi

同步式智能阅读系统
解决了痛点

鼠标停滞/再某一段逗留很久或者说用户开始频繁上下滚动的看
自动问候说需不需要帮助


