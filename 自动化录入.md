# 需求文档：文献元数据与分区自动补全模块

## 1. 业务背景与目标
* **业务痛点：** 用户导入 PDF 文献后，手动录入标题、期刊名、分区、影响因子等元数据耗时且易错。直接使用纯 OCR 或 LLM 解析 PDF 排版存在极高的不确定性和错误率。
* **核心目标：** 建立一套“多级路由降级”的自动化流水线。系统通过提取轻量级的唯一标识符（如 DOI、ArXiv ID 等），对接官方学术 API 与本地数据库，实现文献元数据的高精度、自动化填充。
* **设计原则：** 抓大放小，绝对准确优先。优先依赖 API 和结构化数据，对边缘/非标文献采用“优雅降级”策略并引入人工校验。

---

## 2. 系统总流程设计（路由策略）

系统在接收到新的文献输入（PDF 或 PDF + 引文文件）后，将按以下优先级进行路由分发：

1.  **前置判断：** 检查是否随附了结构化引文文件（如 `.ris`、`.bib`）。如果存在，优先走【中文/非标文献处理模块】进行直接解析。
2.  **标识符扫描：** 对 PDF 前 3 页文本执行正则表达式扫描，寻找唯一标识符。
    * 匹配到 `10.xxxx/xxxx` -> 路由至【标准国际期刊处理模块（SCI流水线）】。
    * 匹配到 `arXiv:xxxx` 或检测为计算机顶会格式 -> 路由至【计算机科学会议处理模块（CS流水线）】。
3.  **LLM 兜底扫描：** 若未扫描到任何标识符，调用大语言模型（LLM）提取标题和作者。
    * 若是英文 -> 尝试走【CS流水线】的模糊匹配。
    * 若是中文 -> 路由至【中文/非标文献处理模块】，并标记为“需人工复核”。

---

## 3. 核心功能模块说明

### 模块 A：标准国际期刊处理模块（SCI/SSCI 流水线）
**适用对象：** 具有标准 DOI 的传统自然科学、医学、工程类期刊文献。

* **输入：** 论文 DOI 字符串。
* **数据获取（API 调用）：**
    * 调用 **Crossref API** 或 **Semantic Scholar API** 获取论文元数据。
    * 需提取的核心字段：`title`（标题）、`authors`（作者）、`container-title`（期刊全称）、`ISSN`（唯一期刊代码）、`published-print`（出版年份）。
* **分区与因子匹配：**
    * **严禁**使用期刊名字符串匹配分区表。
    * 必须使用 API 返回的 `ISSN` 字段，查询本地预置的【中科院分区及 JCR 数据库】，获取并回填该论文的“中科院大类/小类分区”和“影响因子（IF）”。
* **输出：** 完整的结构化条目数据。

### 模块 B：计算机科学会议处理模块（CS 流水线）
**适用对象：** 计算机科学领域的正式会议论文、预印本（ArXiv）、OpenReview 稿件。

* **输入：** ArXiv ID、DBLP Key、OpenReview ID 或（标题+作者）。
* **数据获取（API 调用）：**
    * 强制指定调用 **Semantic Scholar (S2) API**，以解决同一论文存在预印本与正式版多个版本的问题。
    * 根据提取到的标识符直接请求；若无标识符，使用论文标题请求 `/graph/v1/paper/search` 端点。
    * 需提取的核心字段：`title`（标题）、`authors`（作者）、`venue`（会议简称，如 CVPR, ACL）、`year`（年份）。
* **分区匹配：**
    * 提取 API 返回的 `venue` 字段。
    * 将 `venue` 与本地预置的【CCF 会议推荐目录数据库】进行字符串全匹配或忽略大小写匹配，获取并回填“CCF-A/B/C”分类标签。
* **输出：** 包含会议简称及 CCF 分类的结构化条目数据。

### 模块 C：中文及非标文献兜底处理模块
**适用对象：** 知网/万方中文文献、无标识符的老旧文献、非核心期刊。

* **策略 1：文件解析（首选）**
    * 若用户上传了从知网导出的 `.ris` 或 `.bib` 文件，系统直接使用开源解析库（如 `rispy` 或 `bibtexparser`）读取字段（标题、作者、中文期刊名），无需扫描 PDF。
* **策略 2：LLM 降级提取（备用）**
    * 向 LLM 发送 PDF 首页文本，执行固定 Prompt：“请提取本文的标题、作者、中文期刊名。若无法确定期刊名，请返回 null。”
* **核心词典匹配：**
    * 拿到“中文期刊名”后，在本地的【北大核心/CSSCI/CSCD 期刊白名单数据库】中进行精确字符串匹配，若命中则打上对应的核心标签。
* **状态控制：** 凡是进入此流水线且未通过策略 1（解析引文文件）获取完美数据的文献，系统必须将其状态字段设置为 `Needs Review`（需人工核验），并在 UI 层以醒目颜色提示用户。

---

## 4. 本地依赖与数据库需求

为了实现断网可用性和查询速度，系统需在本地环境（如 SQLite 或 JSON 词典文件）维护并定期更新以下三张对照表：

1.  **中科院分区与 JCR 基础表：**
    * 字段：`ISSN`, `Journal_Name`, `JCR_Quartile` (Q1-Q4), `CAS_Zone` (1-4区), `Impact_Factor`。
    * 主键/索引：`ISSN`。
2.  **CCF 会议与期刊推荐目录：**
    * 字段：`Venue_Acronym` (简称，如 CVPR), `Full_Name`, `Category` (A/B/C)。
    * 主键/索引：`Venue_Acronym`。
3.  **中文核心期刊白名单库：**
    * 字段：`Journal_Name_CN` (中文名), `Is_PKU_Core` (布尔值), `Is_CSSCI` (布尔值)。
    * 主键/索引：`Journal_Name_CN`。

---

## 5. 异常处理与系统约束

1.  **API 节流与重试：** Crossref 和 Semantic Scholar 虽免费，但有并发限制。系统底层 HTTP 请求必须实现重试机制（Retry-backoff）和速率限制（Rate Limiting，如限制在每秒 3-5 个请求）。
2.  **数据冲突覆盖原则：** 当系统再次抓取已有文献的数据时，对于手动修改过的字段（User Override），系统不得静默覆盖，需保留用户的手动输入。
3.  **解析超时：** 正则表达式扫描或 LLM 提取需设置严格的超时时间（例如 5 秒），超时则自动降级为“未识别”，转交人工处理，防止阻塞整个批量导入队列。