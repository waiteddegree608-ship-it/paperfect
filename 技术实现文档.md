# Paperfect 技术实现文档

## 1. 架构设计
项目采用典型的 FastAPI + SQLite 后端，前端采用 Vue 3 + pdf.js 实现多窗口 iframe 流式交互。

```text
+-------------------------------------------------------------+
|                         Paperfect UI                        |
|   +-------------------+  +-------------------------------+  |
|   |    PPT 视窗       |  |          PDF 视窗             |  |
|   |  (交互式导航大纲) |  |   (包含高亮/翻译/批注等多视图) |  |
|   +-------------------+  +-------------------------------+  |
+-------------------------------------------------------------+
          ^                                ^
          | slide_change 消息              | scroll / page_change 消息
          v                                v
+-------------------------------------------------------------+
|                    PdfScrollSyncManager                     |
|  - 实时维护 slideIndex <-> pageNum 双向映射字典              |
|  - 利用 postMessage 实现跨跨域/跨组件 iframe 联动           |
+-------------------------------------------------------------+
```

## 2. 核心技术实现

### 2.1 双向幻灯片-文献同步联动
双向联动的核心在于 `PdfScrollSyncManager` 以及在解析阶段生成的 `pptPageMapping` 映射字典。
- **PPT 触发 PDF 跳转（PPT -> PDF）**：当用户在 PPT 编辑器中切换幻灯片时，PPT 视窗会向父窗口发送 `slide_change` postMessage 消息。父窗口捕获到 slideIndex 后，查询映射字典，利用 PDF.js API 将对应的 PDF 视窗滚动至对应的页码。
- **PDF 触发 PPT 切换（PDF -> PPT）**：当用户在 PDF 视窗中滚动阅读时，同步管理器监听滚动高度并读取当前 PDF 页码。页码发生变化时，反向检索映射字典，找到对应的 slideIndex，随后向 PPT iframe 发送消息控制 PPT 视窗自动跟随翻页。

### 2.2 多维度生成式 AI 标注
自动标注服务（`pdf_annotator.py`）的逻辑如下：
1. **语义锚点识别**：系统首先提取论文文本，将其与该文献的中文深度解析报告输入大模型，在英文原文中定位一字不差的 5-15 个连续单词作为 Anchor 锚点。
2. **PDF 坐标定位与渲染**：后端利用 PyMuPDF 的 `search_for` 函数在 PDF 的三维坐标系中检索锚点文字，计算出对应的矩形区域（Rects），利用 `annotator` 在 PDF 页面上渲染出原生的 PDF 批注注释。
