# AI Vision Annotator (AI 视觉图表智能解析工具)

AI Vision Annotator 是一款专为学术研讨会（AI 组会）及学术报告量身打造的**全自动架构图解析与智能排版标注工具**。
传统的工具往往需要手动绘制各种连线和注解，而本项目结合了领先的视觉大语言模型（VLM）技术，能够一键对复杂的学术模型架构图进行精准拆解、结构定位，并生成高质量的本地化知识注解。

## ✨ 核心特性 (Key Features)

- **🧠 深度结合文档语境的纯自动化解析** 
  结合外部输入的学术文献（如 Markdown 解析报告），自动识别图像中的重要模块。系统不仅仅停留于“看图说话”，更能基于研报提供深度的专有名词与原理解释。
  
- **🎯 Qwen-VL 自适应标准坐标映射** 
  彻底解决 AI 对图像位置分析容易产生“幻觉”的业界难题。服务端依靠标准化刻度的映射算法（Normalized Coordinate Projection），与前端的原始等比例 1:1 UI 进行深度链接，让射出的箭头犹如精准巡航般直击各个模块核心。
  
- **📏 动态智能白底延展与排版 (Auto-Layout Engine)**
  告别文字重叠灾难。系统会在用户上传学术原图后，自动向下融合出足够庞大的纯百“展览区”。之后后端排版引擎对截取的结构组件从左至右做精细的矩阵重划分，确保生成的数百字解说在任何情况下不会相互遮挡。
  
- **📸 像素级纯净底边导出 (Smart Boundary Truncation)**
  集成基于 HTML2Canvas 的动态深层 DOM 解析重计算算法，系统在出图（PNG导出）时能以毫米级精确切割下方的白边，最后输出一张包裹性完美的高分辨率大图，彻底免疫各种笔记本和显示幕的 CSS `Max-height` 回退缩放错位。

## 🛠️ 技术栈 (Tech Stack)

*   **前端**: React + TypeScript + Vite + Tailwind CSS + Lucide Icons + HTML2Canvas
*   **后端**: Node.js + Express
*   **AI 接口**: Silicon Flow (硅基流动) OpenAI 兼容结构代理
*   **核心模型**: `Qwen/Qwen3-VL-235B-A22B-Thinking`

## 🚀 快速启动指南 (Get Started)

### 1. 安装项目依赖

确保您安装了 **Node.js**，然后在项目根目录下执行安装：

```bash
npm install
```

### 2. 环境及配置说明

默认情况下，由于当前服务专用于某场展示，我们在后端的 `server.js` 中做了两项固定项：
*   **API 密钥**：在 `server.js` 中硬编码了 SiliconFlow 的 API Key。若要在公网/生产环境使用，请务必将其移至 `.env`。
*   **知识背景引入**：系统强制关联了本机的知识图谱文件（默认路径 `E:\workspace\aigal\参考\输出结果_FashionTex.md`）。在分析其他新论文图表前，您可以修改该解析脚本路径使其摄取新的知识内容。

### 3. 一键并发启动

本项目已配置好了强大的无缝并发命令行工具库（Concurrently）。您只需要在终端键入：

```bash
npm run dev
```

在这一个指令下：
- **Vite 客户端** 将自动启动至 `http://localhost:5174` 
- **Node.js 处理后端** 将同步启动至 `http://localhost:3005`

### 4. 使用流程
1. 进入主页，点击 **`Load Image (加载映射)`** 上传需要拆解的源架构图。
2. 点击绿色的 **`Load AI Analysis (加载 AI 分析)`** 按钮，短暂耐心等待 Qwen-VL 视觉超算解析完毕。
3. 如果自动生成的文字或坐标不需要再修改，点击右上角的 **`Export JPG/PNG (导出 PNG)`** 按钮，程序会立刻切除所有冗杂白边并生成终极成稿保存到您的本地！

---
> 这个项目完美诠释了“AI 工具如何反哺 AI 学术建设”。让解读论文的步骤从繁琐的 PPT 绘制回归到了它该有的样子：极度丝滑且极具生产力。
